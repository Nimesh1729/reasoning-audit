project:
  name: reasoning-audit

system:
  seed: 42

data:
  train_csv: data/benchmark/train.csv
  benchmark_csv: data/benchmark/test.csv

model:
  name: google/flan-t5-base

training:
  output_dir: outputs/lora/flan_t5_base_v2
  learning_rate: 0.0001
  num_train_epochs: 5
  per_device_train_batch_size: 2

logging:
  level: INFO
